# Review Checker Frontend

React + Vite frontend for the AI Review Verification System.

## Run

```bash
npm install
npm run dev
```

Set `VITE_API_URL` if the backend is not running at `http://127.0.0.1:8000`.

```env
VITE_API_URL=http://127.0.0.1:8000
```
